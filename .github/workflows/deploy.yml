name: Build and Deploy Hanzo KV

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'deps/**'
      - 'Dockerfile'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/hanzoai/kv:latest
            ghcr.io/hanzoai/kv:${{ github.sha }}
            docker.io/hanzoai/kv:latest
          cache-from: type=gha,scope=kv
          cache-to: type=gha,mode=max,scope=kv

  deploy-hanzo:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Configure kubectl (hanzo-k8s)
        run: doctl kubernetes cluster kubeconfig save do-sfo3-hanzo-k8s

      - name: Rolling update redis
        run: |
          kubectl -n hanzo set image statefulset/redis-master \
            redis=ghcr.io/hanzoai/kv:latest
          kubectl -n hanzo rollout status statefulset/redis-master --timeout=120s

  deploy-lux:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Configure kubectl (lux-k8s)
        run: doctl kubernetes cluster kubeconfig save do-sfo3-lux-k8s

      - name: Rolling update redis
        run: |
          kubectl -n hanzo set image statefulset/hanzo-redis \
            redis=ghcr.io/hanzoai/kv:latest
          kubectl -n hanzo rollout status statefulset/hanzo-redis --timeout=120s
